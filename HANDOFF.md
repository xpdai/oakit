# OAKIT — 交接說明

> 給下一個 session。這份講「為什麼是這樣」和「接下來做什麼」；
> 怎麼跑請看 [`README.md`](./README.md)，寫程式的規矩看 [`CLAUDE.md`](./CLAUDE.md)。

---

## 這是什麼、為什麼做

使用者要規劃「月入五位數（NT$）」的專案。盤點桌面既有專案後，方向定在
**LINE 官方帳號自動化 + 官網整合**。

查證過的市場事實（2026-08）：

- **Reply API 免費不計次，Push API 才計費。** 2025 年 LINE 把免費訊息額度從
  25,000 則砍到 6,000 則，輕用量方案只剩 200 則、加購 $0.2/則起。
  → 推播變貴變沒效（零售業封鎖率已達 40%），**自動回覆卻是免費的**，
  「用自動回覆接住主動來問的人」這個賣點在 2026 年比兩年前強很多。
- **通用型 LINE 工具已是紅海**：漸強實驗室、賴管家等做很久，而且
  **LINE 官方後台自己也內建 AI 聊天機器人了**，基礎自動回覆正在被平台吃掉。
  → 不能做「又一個 LINE 自動回覆工具」。
- **空白在中間**：LINE 工具商只做 LINE 那一半（客戶的官網不是他們的），
  建站工具（Wix 等）完全不碰 LINE。
  → **「官網內容自動成為 LINE AI 客服的知識庫」目前沒人做**，這是本專案的立足點。

---

## 已完成（可跑）

- `tenant.json` 資料模型 + 驗證
- 官網生成（單檔 HTML、支援深色模式、LINE 加好友為主要 CTA）
- 圖文選單：SVG → PNG（sharp）→ LINE API 建立 + 設為預設，含版位自動計算
- 兩段式自動回覆：規則 → AI（Claude）→ 兜底
- 多租戶 webhook（`/webhook/:tenantId`，含簽章驗證）
- CLI：`build` / `richmenu` / `ask` / `serve`
- 四個虛構 Demo 網站：`demo-bistro`（暮火食堂）、`demo-interior`（隅光製所）、
  `demo-pet`（小步寵物美容）、`demo-music`（那莫 好聽）
- `oakit`（**用產品做產品的銷售頁**）

驗證過：四個虛構 Demo 的 tenant 仍是官網、圖文選單和 LINE 知識庫的共同來源；
網站輸出與知識庫／規則式回覆已驗證。真實 LINE webhook 端到端、Reply bot 部署，以及
是否採用 LINE 原生 AI，仍是獨立的下一步工作。

**尚未用真實 LINE 憑證端到端跑過，也沒有 LINE 連線或部署** —— 使用者說他在先前的 EASTERN 專案做過
「申請 OA → Messaging API → token → 圖文選單 → webhook」整條流程，沒問題，
所以這條路的技術風險已排除，只是這個 repo 還沒實跑。

---

## 接下來（依優先序）

1. **拿 EASTERN 的 LINE 憑證實跑一次**：`richmenu` 上傳 + `serve` + cloudflared 轉發，
   確認選單真的出現、自動回覆真的回。這是唯一還沒驗證的環節。
2. **選單圖字級偏小**（上限寫死 96px，四格版上看起來偏空），要調 `src/line/richmenu.ts`
   的 `font-size` 計算。
3. **AI 回覆還沒實測**（需要 `ANTHROPIC_API_KEY`）。用 `npm run ask` 驗。
4. **預約流程還沒做**。目前只導到 LINE 對話，沒有 LIFF 表單。
5. **測試已存在並通過**：`npm test` 目前為 4 個 test files、25/25 tests 通過；
   後續新增功能時再補對應回歸測試。

---

## 商業路線（重要 —— 不要跳級）

使用者一開始問的是「能不能做成 SaaS 讓人自己註冊建站」。**結論是可以，但不要現在做。**

自助 SaaS 會死在一個地方：**「請貼上你的 LINE Channel Access Token」**。
餐廳老闆、診所櫃檯做不到這一步，註冊漏斗會在這裡掉光，而你會花六個月才發現。
加上使用者每週只有 10 小時，撐不起自助 SaaS 的客服量。

所以是階梯，**每一階都有現金流，而且每一階的產出都不會浪費**：

| 階段 | 時間 | 做什麼 | 收費 |
|---|---|---|---|
| **Phase 1** ← 現在 | 0–3 月 | 產生器是**內部武器不對外**；LINE 那段手工幫客戶接（對使用者是幾十分鐘的事） | 建置 NT$32,000 + 月費 NT$2,000–6,000 |
| Phase 2 | 3–9 月 | 開薄後台給既有客戶自己改營業時間 / 公告 | 月費開始有正當性 |
| Phase 3 | 9 月+ | 才考慮自助註冊 | 前提：接線流程簡化到老闆能自己做，或談下 LINE 技術夥伴 |

**Phase 1 的產生器不管走不走到 SaaS 都值錢**，因為它讓交付成本趨近於零。
反過來先做 SaaS，是先燒六個月開發然後發現沒人註冊得完。

### 使用者的條件（規劃時的硬限制）

- 每週可投入 **10 小時以下** ← 最硬的限制，決定了不能同時做三件事
- **沒有硬期限**，追求高上限
- **願意直接面對客戶、談需求、收錢**
- 資源從零：沒有特定產業人脈、沒有受眾、沒有預備資金

達標路徑：15 個基礎代管（NT$2,000）+ 3 個成長方案（NT$6,000）= **NT$48,000/月**，
時間成本約 10–15 小時/月。關鍵是月費要分級，不能全部都是「每月一頁」，
那會把時間吃光。

---

## 桌面其他專案的定位（已盤點）

| 專案 | 定位 |
|---|---|
| `Harness skill` / `agent-harness-native` / `company-factory` | **交付引擎，不是產品**。開發者市場是紅海且需要受眾。藏在後面當武器。 |
| `emdesign` | 榯榯室內設計的真實案例頁（41 張實景照 + 表單 + LINE）。**朋友的案子、無償**，所以是作品集不是案例。使用者選擇先擱置。⚠️ 要公開展示需先取得照片授權。 |
| `new-thing`（暗格 AI 偵探） | 產品賭注，跟 INK DROP 同一類問題：做得出來、沒人知道。擱置。 |
| `hermes` | 自用的本地生活助理。 |
| `程式交易` | 自用。⚠️ **不要當服務賣** —— 代客操作 / 投資建議在台灣需投顧執照。 |
| `game` / `game-ads` | INK DROP，見下。 |

---

## 另一條線：INK DROP（已完成，等使用者去開帳號）

單色數字合成掉落遊戲，TypeScript + Canvas + Capacitor。
GitHub：**https://github.com/xpdai/ink-drop**（private，`main` + `ads-only` 兩分支）

- `~/Desktop/game` = 完整版（`IAP_ENABLED = true`）
- `~/Desktop/game-ads` = 純廣告版（`IAP_ENABLED = false`），**v1.0 送審用這份**
- 兩份只差 `src/monetize/features.ts` 一行，可以 `rsync src/` 互搬

狀態：28 個測試通過、iOS Release 編譯通過並過 `-validate-for-store`、
Android AAB 打包成功、iOS 模擬器實測深淺色都正確。

**卡在只有使用者能做的事**：Apple/Google 開發者帳號、隱私政策網址、
AdMob 真實版位、Bundle ID、keystore。細節在 `game/docs/RELEASE.md`。

⚠️ 時程風險：**Google Play 個人帳號要先跑 12 名測試者 × 連續 14 天封閉測試**
才能申請正式發布。

如果使用者回頭問 INK DROP，先讀 `~/Desktop/game/CLAUDE.md` 的「已知的坑」，
那裡記了三個踩過的雷（iOS 安全區、`setPointerCapture`、`BoardEvent` 欄位同步）。
